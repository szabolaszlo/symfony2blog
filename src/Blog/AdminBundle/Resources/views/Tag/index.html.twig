{% extends 'AdminBundle::layout.html.twig' %}

{% block title %}Tags list{% endblock %}

{% block section %}
    <h1>Tags list</h1>

    <table>
        <thead>
            <tr>
                <th>Name</th>
                <th>Slug</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for tag in tags %}
            <tr>
                <td><a href="{{ path('blog_admin_tag_edit', { 'id': tag.id }) }}">{{ tag.name }}</a></td>
                <td>{{ tag.slug }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('blog_admin_tag_show', { 'id': tag.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('blog_admin_tag_new') }}">Create a new tag</a>
        </li>
    </ul>
{% endblock %}
